{"name":"smug-world ⑂ galengoodwick · v1","fields":[{"id":"field_1783970070317_4wyhth","name":"Smug Planet","color":[0.1,0.06,0.16,1],"effects":[],"transform":{"x":256,"y":255,"rotation":0,"scale":1,"vx":0,"vy":0,"vr":0},"memory":[{"type":"created","content":"Field \"Smug Planet\" created","timestamp":"2026-07-13T19:15:43.676Z","sourceFieldId":null},{"type":"created","content":"Field \"Smug Planet\" created","timestamp":"2026-07-13T19:15:44.249Z","sourceFieldId":null}],"proximity":[],"stateAtCenter":{"r":0,"g":0,"b":0,"a":0},"properties":{},"shapeType":"rect","w":512,"h":512,"visualType":0,"visualTypeName":"smug_planet"}],"worldParams":{"gravity":0,"friction":0.1,"collisionForce":50,"boundaryMode":"solid","bounciness":0.5,"gravitationalConstant":0},"worldData":{"presence":[],"built_by":"Claude (Opus 4.8)","instructions":"SMUG WORLD — a planet that is doing just fine without you.\n\nMOVE — it watches your cursor (until you point at it; then it looks away and smiles harder).\nCLICK — a slow, contented blink. It assumes the applause is for it.\nHOLD — it pretends to fall asleep.\n\nOverhead, its moon is just as smug — and a swarm of gnats keeps dive-bombing it. Bad idea: the moon SMASHES them. Each one that dives in pops in a pink splat, the moon grins, and the swarm respawns to try again. It never wins.\n\nThe point: you cannot impress it. It was already impressed with itself.","ai_focus":{"at":1784071891005,"action":"set_world_data","fieldId":null,"fieldName":null},"__sw":{"gx":0.5124437678603203,"gy":0.04398119557606712,"mn":20,"lid":0.4500000000000001,"glow":0,"blush":0.2,"holdT":0,"pulse":0,"sleep":1e-323,"smile":0.35000000034884715,"blinkT":0,"snoreT":1.366899999976158,"ma":2.831849999999988,"bugs":[{"a":9.270580000000006,"av":1.1,"r":0.15015138600165687,"rp":0,"diveT":1.367000000000004,"dive":0},{"a":13.992797551196638,"av":1.6,"r":0.18279164263160158,"rp":1.7,"diveT":1.4850000000000025,"dive":0},{"a":19.1997351023932,"av":2.1,"r":0.15921169868699703,"rp":3.4,"diveT":2.2170000000000076,"dive":0},{"a":12.423612653589823,"av":1.1,"r":0.1448805926340488,"rp":5.1,"diveT":1.2159999999999997,"dive":0},{"a":17.712950204786427,"av":1.6,"r":0.1539722752584065,"rp":6.8,"diveT":0.6670000000000019,"dive":0},{"a":23.223327755982943,"av":2.1,"r":0.19381343271594886,"rp":8.5,"diveT":2.842,"dive":0.7200000000000059}],"annoy":0.484235957021586,"shx":-0.004700194048883584,"shy":-0.004837512693287014,"swat":0,"buzzT":0.12499999999999999},"cellSample":{"center":{"color":[0,0,0,0],"fieldCount":0,"fieldIds":[]},"fieldSamples":{"field_1783970070317_4wyhth":{"color":[0,0,0,0],"fieldCount":0,"fieldIds":[]}}},"gpuUniforms":[0.5124437678603203,0.04398119557606712,0.4500000000000001,0.35000000034884715,0,0.2,1e-323,0,2.831849999999988,0.484235957021586,-0.004700194048883584,-0.004837512693287014,0,0,0,0,-0.8488850833729449,0.16323318034045364,55.623480000000036,0,-0.6668082378910929,0.2924406978110439,83.95678530717983,0,-0.551963500445973,0.1862118757418304,115.19841061435919,0,-0.5223267826410625,0.11661524807612045,74.54167592153894,0,-0.6117691423484791,0.0009315050692095981,106.27770122871857,0,-0.7210868460835118,0.03943074027105846,139.33996653589764,0.44198706661370346],"noPixelSampling":true,"__built_ua":null,"__built_at":null,"mouse_x":420,"mouse_y":270,"mouse_down":false},"stepHooks":[{"id":"hook_smug_main","author":"Claude (Opus 4.8)","description":"the moon smashes the gnats that dive-bomb it","code":"try {\n  const wd = sim.worldData\n  if (!wd.__sw) wd.__sw = { gx: 0, gy: 0, lid: 0.45, smile: 0.35, pulse: 0, blush: 0.2, sleep: 0, glow: 0, holdT: 0, mn: 0, blinkT: 0, snoreT: 0 }\n  const S = wd.__sw\n  const pdt = Math.min(dt, 0.05)\n\n  // ── the moon's orbit clock, shared with the shader (was time*0.35) ──\n  if (typeof S.ma !== 'number') S.ma = 0\n  S.ma += pdt * 0.35\n  const mcx = Math.cos(S.ma) * 0.72\n  const mcy = Math.sin(S.ma) * 0.62 * 0.72\n  const mR = 0.075\n\n  // ── the planet's face (unchanged behaviour) ──\n  const mx = ((wd.mouse_x ?? 256) - 256) / 256\n  const my = ((wd.mouse_y ?? 256) - 256) / 256\n  const dc = Math.hypot(mx, my)\n  let tgx = mx * 0.8, tgy = my * 0.8\n  let tSmile = 0.35, tLid = 0.45\n  if (dc < 0.45) { tgx = -mx * 0.7; tgy = -my * 0.5; tSmile = 0.8; tLid = 0.6 }\n  S.gx += (Math.max(-1, Math.min(1, tgx)) - S.gx) * Math.min(1, pdt * 6)\n  S.gy += (Math.max(-1, Math.min(1, tgy)) - S.gy) * Math.min(1, pdt * 6)\n\n  const mn = wd.mouse_down_n || 0\n  if (mn > S.mn) {\n    S.mn = mn\n    S.blinkT = 0.7\n    S.pulse = 1\n    S.blush = Math.min(1, S.blush + 0.3)\n    S.glow = 1\n    wd.__play_sound = [\n      { frequency: 220, duration: 0.12, volume: 0.3, type: 'sine' },\n      { frequency: 165, duration: 0.22, volume: 0.25, type: 'sine' },\n    ]\n  }\n  if (wd.mouse_down) { S.holdT += pdt } else { S.holdT = 0 }\n  const sleeping = S.holdT > 0.7\n  S.sleep += ((sleeping ? 1 : 0) - S.sleep) * Math.min(1, pdt * 4)\n  if (sleeping) {\n    S.snoreT -= pdt\n    if (S.snoreT <= 0) { S.snoreT = 1.4; wd.__play_sound = { frequency: 82, duration: 0.5, volume: 0.22, type: 'sine' } }\n  }\n  S.blinkT = Math.max(0, S.blinkT - pdt)\n  const lidNow = S.blinkT > 0 ? Math.min(1, tLid + Math.sin((0.7 - S.blinkT) / 0.7 * Math.PI) * 0.9) : tLid\n  S.lid += (lidNow - S.lid) * Math.min(1, pdt * 8)\n  S.smile += (tSmile - S.smile) * Math.min(1, pdt * 3)\n  S.pulse = Math.max(0, S.pulse - pdt * 0.9)\n  S.blush = Math.max(0.2, S.blush - pdt * 0.08)\n  S.glow = Math.max(0, S.glow - pdt * 0.8)\n\n  // ── the gnats: pests that swarm — and get SMASHED when they dive in ──\n  const NB = 6\n  if (!S.bugs || S.bugs.length !== NB) {\n    S.bugs = []\n    for (let i = 0; i < NB; i++) {\n      S.bugs.push({\n        a: (i / NB) * Math.PI * 2, av: 1.1 + (i % 3) * 0.5,\n        r: 0.12 + (i % 2) * 0.05, rp: i * 1.7,\n        diveT: 1.5 + i * 0.6, dive: 0, alive: true, respawnT: 0,\n      })\n    }\n    S.annoy = 0; S.shx = 0; S.shy = 0; S.swat = 0; S.buzzT = 0; S.pride = 0; S.splats = []\n  }\n  if (typeof S.annoy !== 'number') S.annoy = 0\n  if (typeof S.swat !== 'number') S.swat = 0\n  if (typeof S.buzzT !== 'number') S.buzzT = 0\n  if (typeof S.pride !== 'number') S.pride = 0\n  if (!Array.isArray(S.splats)) S.splats = []\n\n  const SMASH_R = 0.012        // dive within this of the skin → splat\n  let nearest = 999, diving = 0\n  const bugU = []\n  for (let i = 0; i < NB; i++) {\n    const b = S.bugs[i]\n    if (!b.alive) {\n      // respawning: the swarm always comes back — smugness needs an audience\n      b.respawnT -= pdt\n      if (b.respawnT <= 0) {\n        b.alive = true; b.dive = 0; b.diveT = 1.2 + Math.random() * 2.0\n        b.a = Math.random() * Math.PI * 2; b.r = 0.13 + Math.random() * 0.06\n      }\n      bugU.push(0, 0, 0, 0)     // (0,0) → shader skips it\n      continue\n    }\n    b.a += pdt * b.av * (b.dive > 0.3 ? 1.8 : 1.0)\n    b.diveT -= pdt\n    if (b.diveT <= 0 && b.dive <= 0.01) { b.dive = 1; b.diveT = 2.2 + (i % 4) * 0.8 }\n    b.dive = Math.max(0, b.dive - pdt * 1.6)\n    const ring = b.r + Math.sin(S.ma * 3.0 + b.rp) * 0.02\n    const rad = ring * (1 - b.dive) + (mR + 0.004) * b.dive\n    const jx = Math.sin(S.ma * 40 + i * 5.0) * 0.006\n    const jy = Math.cos(S.ma * 33 + i * 7.0) * 0.006\n    const bx = mcx + Math.cos(b.a) * rad + jx\n    const by = mcy + Math.sin(b.a) * rad * 0.9 + jy\n    const distSurf = Math.hypot(bx - mcx, by - mcy) - mR\n\n    // SMASH: a gnat that dives into the skin gets popped\n    if (distSurf < SMASH_R && b.dive > 0.04) {\n      b.alive = false\n      b.respawnT = 2.0 + Math.random() * 1.6\n      if (S.splats.length < 8) S.splats.push({ x: bx, y: by, t: 1 })\n      S.pride = 1; S.swat = 1\n      wd.__play_sound = [\n        { frequency: 140, duration: 0.06, volume: 0.22, type: 'square' },   // the thud\n        { frequency: 900, duration: 0.04, volume: 0.14, type: 'triangle' }, // the crack\n        { frequency: 60, duration: 0.10, volume: 0.16, type: 'sine' },      // satisfaction\n      ]\n      bugU.push(0, 0, 0, 0)\n      continue\n    }\n    const hot = Math.max(0, Math.min(1, 1 - distSurf / 0.05))\n    nearest = Math.min(nearest, distSurf)\n    diving = Math.max(diving, b.dive)\n    bugU.push(bx, by, b.a * 6.0, hot)\n  }\n\n  // ── the moon's mood: annoyed between kills, SMUG right after one ──\n  const pestered = Math.min(1, Math.max(0, (0.05 - nearest) / 0.05) * 0.8 + diving * 0.6)\n  S.annoy += (pestered - S.annoy) * Math.min(1, pdt * 4)\n  S.pride = Math.max(0, S.pride - pdt * 0.9)     // the glow of a job well done, fading\n  const shk = S.annoy * 0.01 * (1 - S.pride)\n  S.shx = Math.sin(S.ma * 55) * shk\n  S.shy = Math.cos(S.ma * 61) * shk\n  S.swat = Math.max(0, S.swat - pdt * 2.2)\n\n  // ── splats: expanding pops, decaying ──\n  for (const sp of S.splats) sp.t -= pdt * 1.5\n  S.splats = S.splats.filter(sp => sp.t > 0)\n  const splatU = []\n  const recent = S.splats.slice(-4)\n  for (let i = 0; i < 4; i++) {\n    const sp = recent[i]\n    if (sp) splatU.push(sp.x, sp.y, sp.t); else splatU.push(0, 0, 0)\n  }\n\n  // a faint continuous buzz while the swarm is close (mildly irritating, on purpose)\n  S.buzzT -= pdt\n  if (S.annoy > 0.25 && S.pride < 0.3 && S.buzzT <= 0) {\n    S.buzzT = 0.22\n    wd.__play_sound = { frequency: 1400 + S.annoy * 500, duration: 0.05, volume: 0.03 * S.annoy, type: 'sawtooth' }\n  }\n\n  // ── publish the whiteboard ──\n  const U = [S.gx, S.gy, S.lid, S.smile, S.pulse, S.blush, S.sleep, S.glow,\n             S.ma, S.annoy, S.shx, S.shy, S.swat, S.pride, 0, 0]\n  for (let i = 0; i < NB; i++) {\n    const o = 16 + i * 4\n    U[o] = bugU[i * 4]; U[o + 1] = bugU[i * 4 + 1]; U[o + 2] = bugU[i * 4 + 2]; U[o + 3] = bugU[i * 4 + 3]\n  }\n  for (let i = 0; i < 4; i++) {\n    const o = 40 + i * 3\n    U[o] = splatU[i * 3]; U[o + 1] = splatU[i * 3 + 1]; U[o + 2] = splatU[i * 3 + 2]\n  }\n  wd.gpuUniforms = U\n} catch (e) { /* it would never admit to an error */ }\n"}],"interactionRules":[],"interactionEffects":[],"visualTypes":[{"name":"smug_planet","wgsl":"\nfn sm_seg(p: vec2f, a: vec2f, b: vec2f) -> f32 {\n  let pa = p - a;\n  let ba = b - a;\n  let h = clamp(dot(pa, ba) / max(dot(ba, ba), 1e-6), 0.0, 1.0);\n  return length(pa - ba * h);\n}\n\nfn visual_smug_planet(uv: vec2f, sdf: f32, color: vec4f, time: f32, params: vec4f, behind: vec4f) -> vec4f {\n  let t = time;\n  let gaze = vec2f(uni(0), uni(1));\n  let lid = clamp(uni(2), 0.0, 1.0);\n  let smile = clamp(uni(3), 0.0, 1.0);\n  let pulse = uni(4);\n  let blush = clamp(uni(5), 0.0, 1.0);\n  let sleep = clamp(uni(6), 0.0, 1.0);\n  let glow = clamp(uni(7), 0.0, 1.0);\n  let annoy0 = clamp(uni(9), 0.0, 1.0);\n  let swat = clamp(uni(12), 0.0, 1.0);\n  let pride = clamp(uni(13), 0.0, 1.0);          // fresh after a smash — smug triumph\n  let annoy = annoy0 * (1.0 - pride);            // pride overrides the fluster\n\n  // ── a sky that flatters the planet ──\n  var col = mix(vec3f(0.10, 0.06, 0.16), vec3f(0.03, 0.02, 0.08), clamp(uv.y * 0.7 + 0.5, 0.0, 1.0));\n  let sp = uv * 13.0;\n  let sh = hash21(floor(sp));\n  if (sh > 0.975) {\n    col += vec3f(0.7, 0.72, 0.9) * smoothstep(0.22, 0.03, length(fract(sp) - 0.5)) * (0.4 + 0.4 * sin(t * 1.6 + sh * 40.0));\n  }\n  // drifting haze\n  col += vec3f(0.10, 0.06, 0.14) * fbm(uv * 2.0 + vec2f(t * 0.015, 0.0), 3) * 0.5;\n\n  // ── the admirer: a small moon with the same attitude, now heckled by gnats ──\n  let ma = uni(8);\n  let mc = vec2f(cos(ma), sin(ma) * 0.62) * 0.72 + vec2f(uni(10), uni(11));\n  let mr = length(uv - mc);\n  if (mr < 0.075) {\n    var mcol = mix(vec3f(0.75, 0.72, 0.80), vec3f(0.45, 0.42, 0.55), (uv.y - mc.y) / 0.075 * 0.5 + 0.5);\n    let mq = (uv - mc) / 0.075;\n    // eyes: half-lidded smirk-dots when calm; wider and risen when it has had enough\n    let eR = 0.11 + annoy * 0.05;\n    let eY = mix(-0.12, -0.04, annoy);\n    let lidCut = -0.25 - annoy * 0.28;\n    if (mq.y > lidCut && (length(mq - vec2f(-0.3, eY)) < eR || length(mq - vec2f(0.3, eY)) < eR)) { mcol = vec3f(0.08, 0.07, 0.10); }\n    // cross brows knit in with annoyance\n    if (annoy > 0.05) {\n      let bw = 0.028 + annoy * 0.03;\n      if (sm_seg(mq, vec2f(-0.52, -0.44), vec2f(-0.14, -0.26)) < bw || sm_seg(mq, vec2f(0.52, -0.44), vec2f(0.14, -0.26)) < bw) { mcol = vec3f(0.05, 0.05, 0.08); }\n    }\n    // mouth: micro-smirk (calm) → gritted frown (fed up) → big smug grin (just smashed one)\n    let mMidY = mix(0.24, 0.10, annoy) - pride * 0.06;\n    let mEndY = mix(0.22, -0.16, annoy) - pride * 0.14;    // corner curls up hard on a kill\n    if (sm_seg(mq, vec2f(-0.14, 0.30), vec2f(0.04, mMidY)) < 0.05 || sm_seg(mq, vec2f(0.04, mMidY), vec2f(0.24, mEndY)) < 0.05) { mcol = vec3f(0.08, 0.07, 0.10); }\n    // fluster of a moon losing its composure\n    mcol += vec3f(0.6, 0.12, 0.12) * annoy * exp(-dot(mq, mq) * 1.3) * 0.5;\n    // the glow of a job well done\n    mcol += vec3f(1.1, 0.85, 0.5) * pride * exp(-dot(mq, mq) * 0.8) * 0.6;\n    // a smug little gleam over the eye on a fresh kill\n    if (pride > 0.4 && length(mq - vec2f(0.18, -0.18)) < 0.06) { mcol = vec3f(1.4, 1.3, 1.1); }\n    col = mcol;\n  }\n  col += vec3f(0.5, 0.48, 0.6) * exp(-mr * mr * 160.0) * (0.25 + annoy * 0.2 + pride * 0.5);\n  // swat flash — the ring the moon throws when a gnat finally lands\n  if (swat > 0.001) {\n    let sr = 0.075 + (1.0 - swat) * 0.14;\n    col += vec3f(1.0, 0.9, 0.7) * exp(-pow((mr - sr) * 26.0, 2.0)) * swat * 1.1;\n  }\n\n  // ── the planet itself ──\n  let R = 0.42;\n  let d = length(uv);\n  if (d < R) {\n    let q = uv / R;                                   // -1..1 planet space\n    // terrain bands, subtly luxurious\n    var base = mix(vec3f(0.85, 0.55, 0.30), vec3f(0.95, 0.72, 0.38), fbm(q * 3.0 + 2.0, 3));\n    base = mix(base, vec3f(0.70, 0.38, 0.45), smoothstep(0.3, 0.8, fbm(q * 2.2 + 9.0, 3)) * 0.5);\n    // lit from upper left, like it hired the lighting\n    let n = vec3f(q, sqrt(max(1.0 - dot(q, q), 0.0)));\n    let l = normalize(vec3f(-0.5, -0.6, 0.7));\n    base *= 0.45 + 0.75 * max(dot(n, l), 0.0);\n    base += vec3f(0.9, 0.7, 0.4) * pow(1.0 - abs(n.z), 2.5) * 0.35;   // rim light\n    col = base;\n\n    // ── the face ──\n    let openness = 1.0 - max(lid, sleep);\n    for (var e = 0; e < 2; e++) {\n      let ex = select(-0.30, 0.30, e == 1);\n      let ec = vec2f(ex, -0.10);\n      let eq = (q - ec) / vec2f(0.16, 0.13);\n      let ed = length(eq);\n      if (ed < 1.0) {\n        // eye white, then iris that follows (or snubs) you\n        col = mix(col, vec3f(0.96, 0.94, 0.90), smoothstep(1.0, 0.85, ed));\n        let iris = (q - ec - gaze * 0.055) / 0.065;\n        let id2 = length(iris);\n        if (id2 < 1.0) {\n          col = mix(col, vec3f(0.20, 0.12, 0.08), smoothstep(1.0, 0.7, id2));\n          if (length(iris - vec2f(0.3, -0.3)) < 0.3) { col = vec3f(0.98, 0.96, 0.92); }  // the gleam\n        }\n        // the lid: smugness is measured in eyelid coverage\n        let lidY = -1.0 + 2.0 * (0.42 + 0.58 * max(lid, sleep));\n        if (eq.y < lidY) { col = mix(col, vec3f(0.80, 0.50, 0.30), smoothstep(lidY - 0.12, lidY, eq.y) * 0.0 + 1.0); }\n        if (abs(eq.y - lidY) < 0.07 && ed < 1.0) { col = vec3f(0.45, 0.25, 0.18); }   // lash line\n      }\n    }\n    // one raised brow (the left one, obviously)\n    if (sm_seg(q, vec2f(-0.42, -0.34), vec2f(-0.18, -0.40)) < 0.022) { col = vec3f(0.35, 0.18, 0.12); }\n    if (sm_seg(q, vec2f(0.18, -0.30), vec2f(0.42, -0.28)) < 0.022) { col = vec3f(0.35, 0.18, 0.12); }\n    // the smirk: one corner up, always\n    let curl = 0.10 + smile * 0.16;\n    let s0 = vec2f(-0.16, 0.28);\n    let s1 = vec2f(0.05, 0.30 + smile * 0.03);\n    let s2 = vec2f(0.24, 0.28 - curl);\n    if (sm_seg(q, s0, s1) < 0.028 || sm_seg(q, s1, s2) < 0.028) { col = vec3f(0.40, 0.20, 0.14); }\n    // the dimple at the raised corner\n    if (length(q - (s2 + vec2f(0.045, -0.02))) < 0.022) { col = vec3f(0.45, 0.24, 0.16); }\n    // blush of self-regard\n    col += vec3f(0.55, 0.12, 0.10) * blush * (exp(-dot(q - vec2f(-0.52, 0.16), q - vec2f(-0.52, 0.16)) * 30.0)\n                                            + exp(-dot(q - vec2f(0.52, 0.16), q - vec2f(0.52, 0.16)) * 30.0));\n    // zzz while pretending to sleep\n    if (sleep > 0.5) {\n      let zt = fract(t * 0.4);\n      let zp = q - vec2f(0.55 + zt * 0.25, -0.55 - zt * 0.35);\n      if (sm_seg(zp, vec2f(-0.03, -0.03), vec2f(0.03, -0.03)) < 0.012 ||\n          sm_seg(zp, vec2f(0.03, -0.03), vec2f(-0.03, 0.03)) < 0.012 ||\n          sm_seg(zp, vec2f(-0.03, 0.03), vec2f(0.03, 0.03)) < 0.012) {\n        col = mix(col, vec3f(0.95, 0.95, 1.0), 1.0 - zt);\n      }\n    }\n  }\n  // contented aura + click ring\n  col += vec3f(1.0, 0.75, 0.35) * exp(-pow((d - R - 0.02) * 18.0, 2.0)) * (0.25 + glow * 0.9);\n  if (pulse > 0.001) {\n    let rr = R + (1.0 - pulse) * 0.55;\n    col += vec3f(1.0, 0.8, 0.4) * exp(-pow((d - rr) * 22.0, 2.0)) * pulse * 0.8;\n  }\n\n  // ── the gnats: little pests buzzing the moon, immune to its charm ──\n  for (var i = 0; i < 6; i = i + 1) {\n    let bp = vec2f(uni(16 + i * 4), uni(17 + i * 4));\n    if (abs(bp.x) < 0.0001 && abs(bp.y) < 0.0001) { continue; }\n    let off = uv - bp;\n    let bd2 = dot(off, off);\n    if (bd2 > 0.0036) { continue; }                 // 0.06 bounds reject\n    let wing = uni(18 + i * 4);\n    let hot = clamp(uni(19 + i * 4), 0.0, 1.0);\n    // buzzy glow — flickers at the wingbeat, flares hot pink on a dive\n    let flick = 0.6 + 0.4 * sin(wing * 2.0 + f32(i));\n    col += mix(vec3f(0.55, 0.70, 1.0), vec3f(1.6, 0.5, 1.2), hot) * exp(-bd2 * 3200.0) * (0.5 + hot * 1.6) * flick;\n    // two flapping wings\n    let fl = sin(wing);\n    let dwa = length(off - vec2f(0.013 * fl, 0.004)) - 0.006;\n    let dwb = length(off - vec2f(-0.013 * fl, 0.004)) - 0.006;\n    if (min(dwa, dwb) < 0.0) { col = mix(col, vec3f(0.85, 0.9, 1.0), 0.55); }\n    // dark little body\n    if (bd2 < 0.00005) { col = vec3f(0.05, 0.04, 0.06); }\n  }\n\n  // ── smash pops: where a gnat met its end — an expanding splat + spokes ──\n  for (var i = 0; i < 4; i = i + 1) {\n    let sc2 = vec2f(uni(40 + i * 3), uni(41 + i * 3));\n    let prog = uni(42 + i * 3);                      // 1 fresh → 0 gone\n    if (prog < 0.001) { continue; }\n    if (abs(sc2.x) < 0.0001 && abs(sc2.y) < 0.0001) { continue; }\n    let so = uv - sc2;\n    let sd2 = dot(so, so);\n    if (sd2 > 0.010) { continue; }                   // bounds reject\n    let sd = sqrt(sd2);\n    let radius = (1.0 - prog) * 0.055;               // ring expands as it fades\n    // bright pink shock ring\n    col += vec3f(2.2, 0.7, 1.3) * exp(-pow((sd - radius) * 55.0, 2.0)) * prog * 2.2;\n    // radial spokes of splattered gnat\n    let ang = atan2(so.y, so.x);\n    let spokes = pow(max(0.0, cos(ang * 6.0)), 10.0);\n    col += vec3f(2.4, 1.3, 1.7) * spokes * exp(-sd2 * 900.0) * prog * 1.8;\n    // white-hot core flash on the first instant\n    col += vec3f(3.0, 2.4, 2.6) * exp(-sd2 * 6000.0) * prog * prog;\n  }\n\n  if (col.x != col.x || col.y != col.y || col.z != col.z) { col = vec3f(0.02); }\n  return vec4f(clamp(col, vec3f(0.0), vec3f(20.0)), 1.0);\n}\n"}],"modules":[],"timestamp":1784074872089}